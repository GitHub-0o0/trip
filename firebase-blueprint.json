{
  "entities": {
    "SyncPlan": {
      "title": "Synced Travel Plan",
      "description": "A trip plan shared or loaded across multiple devices with a short code",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique auto-generated ID for the plan"
        },
        "code": {
          "type": "string",
          "description": "6-character unique sync and retrieval code"
        },
        "tripPlan": {
          "type": "object",
          "description": "The complete itinerary data payload containing days, budgets, POIs and transits"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO timestamp representing the last save"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO timestamp representing creation time"
        }
      },
      "required": ["id", "code", "tripPlan", "updatedAt", "createdAt"]
    }
  },
  "firestore": {
    "/sync_plans/{planId}": {
      "schema": "SyncPlan",
      "description": "Collection storing shared real-time travel itineraries"
    }
  }
}
